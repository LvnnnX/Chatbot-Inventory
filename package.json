{
  "name": "mcp-chatbot-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run start --prefix backend\" \"npm run start --prefix mobile\"",
    "dev": "npm start",
    "test": "concurrently \"npm test --prefix backend\" \"npm test --prefix mobile\"",
    "install:all": "npm install && npm install --prefix backend && npm install --legacy-peer-deps --prefix mobile"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "concurrently": "^8.2.2"
  }
}
